[project]
name = "bedrock-agent-blueprint"
version = "0.1.0"
description = "A general cloud agent with a resumable Linux workspace"
requires-python = ">=3.10"
dependencies = [
    "strands-agents",
    "bedrock-agentcore",
    "boto3",
]

[dependency-groups]
dev = [
    "pytest",
]
